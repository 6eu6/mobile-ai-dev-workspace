# Cloudflare Pages redirects — processed BEFORE static files.
#
# The root URL "/" is served directly by the Remix Function (functions/[[path]].ts)
# via app/routes/_index.tsx. No static index.html exists in the build output, so
# the Function always handles "/".
#
# Backward-compat: anyone who bookmarked the old /home route is forwarded to /.
/home / 302
